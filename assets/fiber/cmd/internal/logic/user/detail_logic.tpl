package user

import (
	"{{.ProjectName}}/cmd/internal/types"
	"github.com/gofiber/fiber/v2"
)

func DetailLogic(c *fiber.Ctx, req *types.DetailReq) (rsp *types.DetailRsp, err error) {
	// todo write logic

	return
}